export type HomeProduct = {
  id: string;
  name: string;
  imageUrl: string;
  price: string;
};

export type Product = {
  id: string;
  name: string;
  imageUrl: string;
  price: string;
  description: string;
  defaultPriceId: string;
};
